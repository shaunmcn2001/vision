import { test, expect } from 'vitest'

test('CSS configuration should not cause build errors', () => {
  // This test exists to verify the CSS setup doesn't cause import errors
  expect(true).toBe(true)
})