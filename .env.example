# NDVI Vision - Environment Configuration Template
# Copy this file to .env.local and configure your values

# Backend API URL (required)
VITE_BACKEND_URL=https://vision-backend-0l94.onrender.com

# Optional API Key (if your backend requires authentication)
# VITE_API_KEY=your-api-key-here

# Development Notes:
# - Use .env.local for local development (ignored by git)
# - For GitHub Pages deployment, set these as repository secrets
# - Environment variables must start with VITE_ to be accessible in the frontend